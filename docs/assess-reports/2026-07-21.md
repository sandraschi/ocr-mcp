# OCR-MCP Assessment Report

**Date:** 2026-07-21
**Repo Type:** Standard MCP Server
**SOTA Score:** 34/100 (<60 — runt, needs urgent fixes)

## Summary

Per-severity count: CRITICAL=3, HIGH=10, MEDIUM=6, LOW=2

## CRITICAL Issues

1. **No .env.example** — missing template for required env vars
2. **run_http_async in transport.py** — drops custom middlewares; must use uvicorn.Server on mcp.http_app()
3. **CORS missing allow_origin_regex** — Tailscale *.ts.net, LAN IPs, CGNAT not covered

## HIGH Issues

4. No `## Return Format` in tool docstrings — 0/8 tools comply
5. No `## Examples` in tool docstrings — 0/8 tools comply
6. No `ctx: Context` on any tool — prevents sampling, progress, logging
7. No `ctx.sample()` usage — declared capability never used
8. No tool annotations (READ_ONLY/MUTATING/DESTRUCTIVE) on any tool
9. No Prefab App tools (@mcp.tool(app=True)) despite prefab-ui in core deps
10. No `color-scheme: dark` — native form controls may render with light theme
11. No `.cursorrules` — session context injection for Cursor missing
12. CORS stale port entry — localhost:10818 in allow_origins (copy-paste)
13. No dialogic return pattern — tools don't return {"success","message","data"}

## MEDIUM Issues

14. No Framer Motion — missing from package.json per WEBAPP_SOTA_STANDARDS
15. No `build-native` recipe in justfile — native/ exists but no build recipe
16. No `gates-green` or `certify` recipe — missing from justfile
17. No Playwright E2E tests — only backend unit tests exist
18. .gitignore missing entries — *.mcpb, native/target/, native/gen/, *.bak not covered
19. .bak dross in repo — 5 .bak files exist in tracked directories

## LOW Issues

20. server.py line 162 bare `except Exception: pass` — should log
21. Missing `serve` recipe (has `run` but not `serve` alias)

## Gate Results

| Gate | Status |
|------|--------|
| ruff check src/ | PASS |
| ruff format --check | PASS (pre-existing format issues in 5 files) |
| tsc --noEmit | PASS |
| vite build | PASS |

## Existing Assets

- All 18 required files present
- 8 MCP tools registered
- 3 resources, 5 prompts
- 26 data-testid attributes across webapp pages
- 14 OCR backends
- Tauri NSIS wrapper (native/)
- CI workflow created
- MCPB manifest and bundle built
- GitHub remote configured
